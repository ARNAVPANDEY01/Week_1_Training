//Create a program to find total_price
import java.util.Scanner;

//Create a class with name TotalpriceofItem

    class TotalpriceofItem{
	    public static void main(String[]args){
		    
			
			//Create an instance of Scanner class to user input
			Scanner input = new Scanner(System.in);

			
			
			//Create an integer variable name unit_price to take user input 
			System.out.println("Enter unitprice of item:");
			int unitprice = input.nextInt();

			
			
			//Create an integer variable name quantity to take user input 
			System.out.print("Enter quantity of item:");
			int quantity = input.nextInt();

			
			
			//Create an integer variable name total_price to compute total price
			int totalprice = unit_price * quantity;
			
			
			//Display the result
			System.out.println("The total purchase price in INR is:" + totalprice + "if the quantity:" + quantity + " and unitprice in INR is:" + uniprice);
		}
	}